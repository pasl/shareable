var loadLinkedIn; \
$(function() { \
  if(linkedinLoaded !== true) { \
    loadLinkedIn(); \
    return $(document).on('page:load', loadLinkedIn); \
  } \
}); \
loadLinkedIn = function() { \
  delete IN; \
  $.getScript('//platform.linkedin.com/in.js', function() {linkedinLoaded = true;}); \
  return { \
    lang: '<%= defined?(locale)? locale.to_s : 'en_US' %>' \
  }; \
};